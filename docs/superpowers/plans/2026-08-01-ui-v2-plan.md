# UI Improvements V2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Provide an immersive futuristic AI loading experience and make buttons fully functional.

**Architecture:** We will use custom CSS keyframes for a sweeping laser effect, Tailwind utility classes for text pulsing, DOM rect measurements for dynamic SVG scaling, and component cleanup for unused placeholders.

**Tech Stack:** React, Tailwind CSS

## Global Constraints

- Keep everything in a single page SPA pattern.
- Follow existing Tailwind styling.
- Ensure the user's specific text string "Sedang berpikir..." and its sequence is maintained exactly.

---

### Task 1: Add Custom Keyframes

**Files:**
- Modify: `client/src/index.css`

**Interfaces:**
- Produces: CSS animation classes `.animate-scan` and `@keyframes scan` to be used by DiagramCanvas.

- [ ] **Step 1: Add CSS Keyframes**

Append the following at the end of `client/src/index.css`:

```css
@keyframes scan {
  0% { transform: translateY(-100%); }
  100% { transform: translateY(1000%); }
}

.animate-scan {
  animation: scan 2s linear infinite;
}
```

- [ ] **Step 2: Commit**

```bash
git add client/src/index.css
git commit -m "feat(ui): add scanning laser css animation"
```

---

### Task 2: Breathing Text Animation

**Files:**
- Modify: `client/src/components/ChatPanel/index.tsx`

**Interfaces:**
- Consumes: The `loadingText` state.

- [ ] **Step 1: Update Loading UI**

Find the `isGenerating` conditional rendering block inside the return statement of `ChatPanel/index.tsx`. Replace the current spinner and text block with a minimalist pulsing text.

Before:
```tsx
                <div className="p-4 flex items-center gap-3 text-zinc-400">
                  <Loader2 className="w-4 h-4 text-sky-400 animate-spin" />
                  <span className="text-sm font-medium">{loadingText}</span>
                </div>
```

After:
```tsx
                <div className="p-4 flex items-center text-zinc-400">
                  <span className="text-sm font-medium animate-pulse text-sky-400">{loadingText}</span>
                </div>
```

- [ ] **Step 2: Remove Unused Icons**

If `Loader2` is no longer used, remove it from the lucide-react imports.

- [ ] **Step 3: Commit**

```bash
git add client/src/components/ChatPanel/index.tsx
git commit -m "feat(ui): replace spinner with breathing loading text"
```

---

### Task 3: Holographic Canvas Laser Effect

**Files:**
- Modify: `client/src/components/DiagramCanvas/index.tsx`

**Interfaces:**
- Consumes: `.animate-scan` from `index.css`.

- [ ] **Step 1: Replace Rendering UI**

In `DiagramCanvas/index.tsx`, locate the `isRendering ? (...)` block. Replace the standard spinner with a full-width scanning laser overlay inside a relative container.

Before:
```tsx
        {isRendering ? (
          <div className="flex flex-col items-center text-zinc-500 gap-4">
            <div className="w-8 h-8 border-4 border-zinc-700 border-t-sky-500 rounded-full animate-spin"></div>
            <p>Rendering Diagram...</p>
          </div>
```

After:
```tsx
        {isRendering ? (
          <div className="w-full h-full relative overflow-hidden flex flex-col items-center justify-center">
            {/* The scanning laser line */}
            <div className="absolute top-0 left-0 w-full h-[2px] bg-sky-400 shadow-[0_0_15px_rgba(56,189,248,1)] animate-scan z-10"></div>
            {/* Background grid pulsing effect */}
            <div className="text-sky-500/50 animate-pulse font-mono text-sm tracking-widest uppercase">
              Generating Blueprint...
            </div>
          </div>
```

- [ ] **Step 2: Commit**

```bash
git add client/src/components/DiagramCanvas/index.tsx
git commit -m "feat(ui): add holographic laser scanning effect to canvas"
```

---

### Task 4: True "Fit to Screen" Logic

**Files:**
- Modify: `client/src/components/DiagramCanvas/index.tsx`

**Interfaces:**
- Consumes: `containerRef` and `svgContent`.

- [ ] **Step 1: Update handleReset logic**

In `DiagramCanvas/index.tsx`, replace the old `handleReset` function. We need to calculate the bounding box of the SVG relative to its parent wrapper container.

Before:
```tsx
  const handleReset = () => setZoom(1);
```

After:
```tsx
  const handleReset = () => {
    if (!containerRef.current) {
      setZoom(1);
      return;
    }
    
    const svgEl = containerRef.current.querySelector('svg');
    const wrapperEl = containerRef.current.parentElement;
    
    if (!svgEl || !wrapperEl) {
      setZoom(1);
      return;
    }

    // Get the base sizes
    const svgRect = svgEl.getBoundingClientRect();
    const wrapperRect = wrapperEl.getBoundingClientRect();
    
    // We want the unscaled dimensions of the SVG for the math.
    // getBoundingClientRect includes the current scale transform, so we divide by current zoom to get native width
    const nativeSvgWidth = svgRect.width / zoom;
    const nativeSvgHeight = svgRect.height / zoom;

    if (nativeSvgWidth === 0 || nativeSvgHeight === 0) {
      setZoom(1);
      return;
    }

    const widthRatio = wrapperRect.width / nativeSvgWidth;
    const heightRatio = wrapperRect.height / nativeSvgHeight;
    
    // Fit to screen with 10% padding
    const idealZoom = Math.min(widthRatio, heightRatio) * 0.9;
    
    // Clamp zoom between 0.2 and 4
    setZoom(Math.max(0.2, Math.min(idealZoom, 4)));
    toast.success('Fitted to screen');
  };
```

- [ ] **Step 2: Commit**

```bash
git add client/src/components/DiagramCanvas/index.tsx
git commit -m "feat(ui): implement dynamic scaling for fit to screen"
```

---

### Task 5: Sidebar Placeholders Cleanup

**Files:**
- Modify: `client/src/components/Sidebar/index.tsx`

**Interfaces:**
- Removes unused anchor tags temporarily.

- [ ] **Step 1: Remove Links**

In `client/src/components/Sidebar/index.tsx`, remove the bottom div containing the placeholder links to avoid user confusion.

Before:
```tsx
      <div className="p-4 border-t border-zinc-800 text-sm flex flex-col gap-2">
        <a href="#" className="hover:text-white transition-colors">Settings</a>
        <a href="#" className="hover:text-white transition-colors">GitHub</a>
        <a href="#" className="hover:text-white transition-colors">About</a>
      </div>
```

After (remove it completely, or comment it out):
```tsx
      {/* 
      TODO: Re-enable when features are built
      <div className="p-4 border-t border-zinc-800 text-sm flex flex-col gap-2">
        <a href="#" className="hover:text-white transition-colors">Settings</a>
        <a href="#" className="hover:text-white transition-colors">GitHub</a>
        <a href="#" className="hover:text-white transition-colors">About</a>
      </div> 
      */}
```

- [ ] **Step 2: Commit**

```bash
git add client/src/components/Sidebar/index.tsx
git commit -m "chore(ui): temporarily hide unimplemented sidebar links"
```
